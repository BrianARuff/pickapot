<h1> All Categories </h1>
  <table class="table table-responsive table-responsive">
  <thead>
    <tr>
      <th>Name</th>
      <th>Username</th>
    </tr>
    </thead>
    <% @categories.order(name: :asc).each do |category| %>
  <tbody>
    <tr>
      <td><%= category.name %></td>
      <td><%= "@#{category.user.user_name}" %></td>
      <td><%= link_to 'Show', category_path(category), class: 'btn btn-primary' %></td>
      <td><%= link_to 'Edit', edit_category_path(category), class: 'btn btn-warning' %></td>
      <td><%= link_to 'Delete', category_path(category), method: :delete, data: {confirm: 'Are you sure you want to delete this category?'}, class: 'btn btn-danger' %></td>
    </tr>
    </tbody>
    <% end %>
  </table>
<%= link_to 'Create New Category', new_category_path, class: 'btn btn-primary' %>